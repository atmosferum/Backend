spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/whattime
    username: postgres
    password: kA+VE!qR6K5jr64m
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  error:
    include-message: always

application:
  auth:
    cookie:
      name: authToken
      maxAgeInDays: 365